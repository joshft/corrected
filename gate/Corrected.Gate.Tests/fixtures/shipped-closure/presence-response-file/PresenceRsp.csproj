<Project Sdk="Microsoft.NET.Sdk">
  <!-- INV-011 fixture (QA-002): a committed Directory.Build.rsp beside the project injects
       MSBuild command-line switches into every build invoked from this dir. The csproj is
       otherwise clean; the response file's PRESENCE => Fail (never built). -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
</Project>
