<Project Sdk="Microsoft.NET.Sdk">
  <!-- INV-011 fixture (QA-002): a <UsingTask> registers a custom build task (arbitrary
       code at build time). Presence policy => Fail (never built). -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <UsingTask TaskName="Injected.CustomTask" AssemblyFile="injected.dll" />
</Project>
