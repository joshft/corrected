namespace WithAllowedRef
{
    // Skeleton-only: no body, no initializer, synthesizes nothing. The allow-branch is
    // exercised by the resolved AllowedRefLib <ProjectReference>, not by this source.
    public sealed class Consumer
    {
        public int Handle { get; }
    }
}
