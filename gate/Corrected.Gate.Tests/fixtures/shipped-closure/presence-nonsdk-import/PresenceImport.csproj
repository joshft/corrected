<Project Sdk="Microsoft.NET.Sdk">
  <!-- INV-011 fixture (QA-002): a non-SDK explicit <Import Project="..."> pulls in
       arbitrary committed MSBuild logic. Presence policy => Fail (never built). -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <Import Project="injected.props" />
</Project>
