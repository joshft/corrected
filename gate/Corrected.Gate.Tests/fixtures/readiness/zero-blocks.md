# INV-001 zero-block fixture: no column-0 `implementation_readiness:` inside a
# ```yaml fence anywhere -> extraction finds ZERO blocks -> hard fail-closed.

Just prose, no readiness block at all. A mention of implementation_readiness in a
sentence does not count.
