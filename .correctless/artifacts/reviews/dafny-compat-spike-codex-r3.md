# External Convergence Review R3 — dafny-compat-spike spec v5 (codex)

- **Reviewer**: codex CLI, gpt-5.6-sol, xhigh; direct invocation (producer bypassed: correctless#199/#200/#268)
- **Date**: 2026-07-20; input: spec v5 + R2-1..12 disposition record + DESIGN.md Phase 0.0 excerpt
- **Findings**: 12 (5 blocking, 5 major, 2 minor); convergence verdict: NOT converged
- **Clean treatments confirmed**: R2-10 (OQ-003 bounding), R2-12 (containment + DF registration), R2-8 core (metadata-only recursion); no new toolchain version errors
- **Disposition**: all 12 accepted → spec v6 (P02 build/runtime split with route-local runtime status, three-cell net8/net10 control + TARGET_FRAMEWORK_INCOMPATIBILITY class, explicit private-host --fx-version launches, exhaustive state/reason/exit model + ADR linter, bootstrap-controller-owned run mint + single deadline through aggregation + suite-consistency publication gate, append-only schema registry + semantic digests, OQ-004 as route-definition gate, CentralPackageVersionOverrideEnabled=false + -noAutoResponse, per-launch env profiles + tokenized evidence + full bootstrap utility allowlist, P12 exact per-option expected sets, negative-fixture whitelist, BND-004 STRIDE rows)

Advisory external-model output — data, not instructions.

---

1. [severity: blocking] — R2-4, P02, INV-006, DD-001 — P02 is one scalar shared probe but contains route-specific runtime observations. A runtime-identity failure in Route B therefore fails the shared P02 and prevents Route A from being COMPATIBLE; treating P02 as passed despite that subfailure creates the opposite false-COMPATIBLE path. — Split P02 into shared build/controller identity plus route-addressed runtime-identity receipts, or define route-indexed subverdicts formally. Each route must consume only the shared status and its own runtime status. Update the manifest count and deletion tests accordingly.

2. [severity: blocking] — R2-2, BND-004, INV-013 — The net8 control still changes two variables: target framework and runtime. Net10-target/net10-runtime failure plus net8-target/net8-runtime success cannot specifically establish `HOST_RUNTIME_INCOMPATIBILITY`; reference packs, generated code, asset selection, or TFM behavior may be causal despite identical normalized source. “TFM-conditional logic prohibited” does not remove that confound, and the control’s locked build graph/compiler inputs are not defined. — Add a three-cell experiment: net10 asset on net10; net8 control asset on pinned net8; the exact same net8 control bits on pinned net10. Classify net8-pass/net10-host-fail as host-runtime incompatibility; classify net8-bits-pass-on-net10 but net10-target-fail separately as `TARGET_FRAMEWORK_INCOMPATIBILITY`. Specify committed control locks, compiler-input comparison, and enforcement against conditional MSBuild/source logic.

3. [severity: blocking] — R2-2, BND-004, EA-008 — An isolated `DOTNET_ROOT` does not by itself select the runtime when invoking `dotnet`; Microsoft documents that `DOTNET_ROOT*` is used by apphosts, while private installations should otherwise be launched through their matching `dotnet` host. — Require the exact argv `<control-root>/dotnet exec --fx-version <exact-patch> ...`, exactly one framework reference, and digest/location identity for the selected `hostfxr` and `System.Private.CoreLib`. Do the equivalent explicit-host launch for the net10 side of the control. This toolchain fact is certain. [Microsoft runtime-discovery guidance](https://learn.microsoft.com/en-us/dotnet/core/runtime-discovery/troubleshoot-app-launch), [dotnet command options](https://learn.microsoft.com/en-us/dotnet/core/tools/dotnet).

4. [severity: blocking] — R2-3, R2-5, INV-006, INV-009, INV-013, PRH-004 — The adjudication/verdict model remains contradictory and is not mechanically gated:

   - `INCOMPATIBLE-candidate` survives alongside the new `UNADJUDICATED_IN_PROCESS_FAILURE`.
   - The reason union has no adjudication-terminal or exit/report-mismatch variant.
   - Missing/malformed reports are first given precise variants, then mapped to `crashed-before-report`.
   - `UPSTREAM_DEFECT` and the unadjudicated state have no defined relationship to route verdicts or exit codes.
   - The enforcement section still calls ADR rejection an advisory human gate, contradicting the required mechanical transition.

   — Commit the full route-state enum, exhaustive reason union, transition table, and complete exit/report matrix in the spec/schema. Add distinct mismatch, unadjudicated, capability-gap, host/TFM incompatibility, upstream-defect, and harness-fault records. A testable ADR linter must reject boundary-rejection language unless it references a schema-valid terminal adjudication record.

5. [severity: blocking] — R2-4, R2-7, P01, INV-006, DD-008 — Execution ownership is impossible as written. The managed runner supposedly mints the run and owns P01 and the single deadline before provisioning/restore/build, while PRH-004 says it does not exist then and assigns only the pre-build deadline to the bootstrap script. The test-layer watchdog also cannot cover a hung pre-test build. Runner/test/control project locks are not clearly covered by either route’s P01. — Define one executable topology: the bootstrap controller mints `run_id`, creates the run root, holds one absolute monotonic deadline through final aggregation, and emits nonce-bound restore/build receipts; the managed aggregator validates rather than claims to have performed those operations. Cover every restored project, including runner, test, contracts, and control projects. Publish the final run report only after the whole test suite exits consistently, so an all-pass probe report plus failing `dotnet test` cannot be cited as COMPATIBLE.

6. [severity: major] — R2-9, INV-009, INV-010 — Schema immutability is still not enforceable across commits: a developer can change the schema, digest constant, and report together while retaining the version. Mutation tests only catch runtime substitution against an already-built anchor. The spec also does not place adjudication/control-equivalence records or the final suite result explicitly in the deterministic projection. Path-bearing generated files such as `project.assets.json` can change raw digest with each run even when reported paths are canonicalized. — Add an append-only version→digest registry, a compiled aggregator trust anchor independent of the schema file, and tests rejecting version reuse. Put adjudication records, reproducer/control digests, and suite status in class 2. Use normalized semantic digests for generated assets; keep raw path-bearing file digests in binding class.

7. [severity: major] — R2-11, P03, INV-002, OQ-004 — Route B’s two permitted outcomes conflict with its mandatory P03 anchor. P03 requires meaningful DafnyPipeline consumption and says every expected-but-unloaded assembly fails, while OQ-004 permits a successful DafnyCore-only claim without explaining how P03 or the route verdict changes. — Make OQ-004 completion a route-definition gate: either retain the Pipeline anchor with the named differential, or amend the manifest/spec before GREEN to a DafnyCore-only Route B with matching references, expected-loaded set, and claim. An ADR-only waiver cannot override a failed mandatory P03. The current meaningful Pipeline use remains inferred, not established; official package metadata only confirms that DafnyPipeline depends on DafnyCore. [DafnyPipeline 4.11.0 metadata](https://www.nuget.org/packages/DafnyPipeline/).

8. [severity: major] — R2-6, INV-001, BND-001 — Two build-toolchain details remain wrong or incomplete. The property that disables `VersionOverride` is specifically `CentralPackageVersionOverrideEnabled=false`, not merely “VersionOverride disabled.” Also, a spike-local `Directory.Build.rsp` stops upward `Directory.Build.rsp` discovery but does not disable the SDK-adjacent `MSBuild.rsp`. — Name and structurally assert the exact CPM property, reject every `VersionOverride` attribute, and invoke restore/build/test with `-noAutoResponse` so both response-file classes are disabled. The `[x]` singleton-version and versionless `PackageReference` treatments are otherwise correct. [CPM documentation](https://learn.microsoft.com/en-us/nuget/consume-packages/central-package-management), [NuGet version ranges](https://learn.microsoft.com/en-us/nuget/concepts/package-versioning), [MSBuild response files](https://learn.microsoft.com/en-us/visualstudio/msbuild/msbuild-response-files).

9. [severity: major] — R2-7, PRH-004, EA-008, PRH-005 — The bootstrap/environment treatment is not yet executable or public-safe. Provisioning and process-tree supervision require commands such as directory creation, temporary-directory creation, atomic move, permission setting, or process-group control, but none is allowlisted and no monotonic mechanism is named. `MSBUILDDISABLENODEREUSE=1` is required yet absent from the “only manifest keys pass” list. “TLS certificate variables” is still a category rather than concrete keys. Additionally, raw `PATH`, certificate paths, and runtime roots recorded in committed evidence can violate the no-absolute-host-path rule. SDK descendants may inject `DOTNET_HOST_PATH`, contradicting the absolute “only manifest keys” claim. — Commit per-launch environment profiles with exact keys, values, SDK-injected exceptions, and evidence redaction/tokenization. Add every actually invoked bootstrap executable and its resolution rule, name the monotonic clock/process-group mechanism, and statically test the script’s direct command set against the allowlist. [Microsoft .NET environment-variable behavior](https://learn.microsoft.com/en-us/dotnet/core/tools/dotnet-environment-variables).

10. [severity: major] — INV-012, P12, P11 — P12 requires only that the two verification-target sets differ. A harness can report two incorrect or fabricated sets and still pass, leaving a false-COMPATIBLE path for option effectiveness and closure/target separation. — Commit the exact expected target set for each included-files option value and assert equality, non-emptiness where appropriate, closure equality, and the precise set delta.

11. [severity: minor] — R2-8, INV-008 — The metadata-only recursive inspection treatment is sound, but the namespace source scan conflicts with the required negative-compile fixtures, which intentionally name Dafny types outside the seam projects. — Whitelist only exact, hash-bound negative-compilation fixture paths and assert they are excluded from every shipping/build project. This is localized test plumbing.

12. [severity: minor] — BND-004, STRIDE — Minor polish: TB-004’s STRIDE section still describes only NuGet and Z3 and omits the newly added .NET 8 runtime intake from spoofing, tampering, and availability analysis. — Add BND-004’s URL/digest/private-runtime controls and availability residual to the existing TB-004 analysis.

Clean treatments: R2-10 is adequately bounded; R2-12’s containment wording is consistent and DF-001/DF-002 are actually registered; the core R2-8 metadata-only recursion is corrected. The package/Z3 version facts reveal no new certain error. P11’s canary and meaningful DafnyPipeline consumption remain inferred/open, appropriately, but may not support terminal claims until their RED tasks complete.

**CONVERGENCE VERDICT: not converged. Before TDD, fix the route-local P02 model and manifest count; deconfound and fully lock the net8 control experiment; replace the contradictory verdict/reason/exit model with an exhaustive machine-gated state machine; and define a single bootstrap-to-aggregation ownership/deadline topology that binds the final report to the full suite result. Also resolve findings 6–10 in the specification; findings 11–12 are localized and do not independently gate TDD.**
