# Workflow History

### 2026-07-22 — .NET 10 / Dafny 4.11.0 Package-Compatibility Spike (Phase 0.0, bullets 1–3)
Branch: feature/dafny-compat-spike. Rules: 19. QA rounds: 3. Findings fixed: 76. Permanent, non-production conformance harness proving Dafny 4.11.0's `net8.0` packages run in-process on a .NET 10 host across two integration routes (both COMPATIBLE, suite-attested 274/274); produced provisional ADR-0001 (Route A selected, promotion pending) and the committed evidence sample pair. High intensity: 3 QA rounds + probe round (mutation + config-fuzz) + 2 mini-audit rounds; 11 findings deferred to backlog (DF-002..DF-012), 1 upstream.

### 2026-07-26 — Readiness-Gate Carrier (phase-0.1-worker enforcement home)
Branch: feature/readiness-gate-carrier. Rules: 26 (18 INV + 8 PRH). QA rounds: 2. Findings fixed: 16. Isolated, non-shipped .NET 10 gate solution under `gate/` (`Corrected.Gate` + `.Kernel` + `.Tests` + `.Lint`, aggregated by `Corrected.Gate.slnx`) that homes the Phase-0.1 worker's readiness gate (INV-001/002/036) and enforces the Stage-A boundary: a pure, I/O-free readiness kernel (READY legal iff every precondition actually satisfied AND every reference resolved), a REAL out-of-process pinned-SDK shipped-closure build scan (INV-011), a stage-partitioned DD-003 migration-consistency gate over current-state anchors + SHA-256 digests, and a repo-root SDK pin — with `P1.satisfied` left `false` and ADR-0001 untouched (the Stage-B flip is deferred). High intensity: 2 QA rounds + probe round + mini-audit. 3 BLOCKING resolved (QA-001 inert DD-003 gate → full enforcement; QA-002 INV-011 static scan → real build; mini-audit kernel forged-READY fail-open); 7 non-blocking fixed (3 QA-r2 + 4 mini-audit) plus QA-006 combined-exit single-source; 4 higher-value residuals fixed pre-commit; 1 /cverify NUL-byte hygiene fix. 4 LOW residuals + 3 drift-debt items (DRIFT-001/002/003) accepted, all dormant at Stage A. Green-from-clean 226/226, GATE_EXIT=0.
