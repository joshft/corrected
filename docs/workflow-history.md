# Workflow History

### 2026-07-22 — .NET 10 / Dafny 4.11.0 Package-Compatibility Spike (Phase 0.0, bullets 1–3)
Branch: feature/dafny-compat-spike. Rules: 19. QA rounds: 3. Findings fixed: 76. Permanent, non-production conformance harness proving Dafny 4.11.0's `net8.0` packages run in-process on a .NET 10 host across two integration routes (both COMPATIBLE, suite-attested 274/274); produced provisional ADR-0001 (Route A selected, promotion pending) and the committed evidence sample pair. High intensity: 3 QA rounds + probe round (mutation + config-fuzz) + 2 mini-audit rounds; 11 findings deferred to backlog (DF-002..DF-012), 1 upstream.

### 2026-07-26 — Readiness-Gate Carrier (phase-0.1-worker enforcement home)
Branch: feature/readiness-gate-carrier. Rules: 26 (18 INV + 8 PRH). QA rounds: 2. Findings fixed: 16. Isolated, non-shipped .NET 10 gate solution under `gate/` (`Corrected.Gate` + `.Kernel` + `.Tests` + `.Lint`, aggregated by `Corrected.Gate.slnx`) that homes the Phase-0.1 worker's readiness gate (INV-001/002/036) and enforces the Stage-A boundary: a pure, I/O-free readiness kernel (READY legal iff every precondition actually satisfied AND every reference resolved), a REAL out-of-process pinned-SDK shipped-closure build scan (INV-011), a stage-partitioned DD-003 migration-consistency gate over current-state anchors + SHA-256 digests, and a repo-root SDK pin — with `P1.satisfied` left `false` and ADR-0001 untouched (the Stage-B flip is deferred). High intensity: 2 QA rounds + probe round + mini-audit. 3 BLOCKING resolved (QA-001 inert DD-003 gate → full enforcement; QA-002 INV-011 static scan → real build; mini-audit kernel forged-READY fail-open); 7 non-blocking fixed (3 QA-r2 + 4 mini-audit) plus QA-006 combined-exit single-source; 4 higher-value residuals fixed pre-commit; 1 /cverify NUL-byte hygiene fix. 4 LOW residuals + 3 drift-debt items (DRIFT-001/002/003) accepted, all dormant at Stage A. Green-from-clean 226/226, GATE_EXIT=0.

### 2026-07-26 — Stage-B P1 flip (readiness gate boundary crossing)
Branch: feature/stage-b-p1-flip. Bounded carrier-verified migration (test-first, no full ctdd — the gate already encodes correctness). Flipped `phase-0-1-worker.md` P1.satisfied→true with a non-null evidence pointer + armed ADR-0001's acceptance schema (`status: accepted`, explicit terminal `supersedes: null`/`superseded_by: null`) + migrated the DD-003 manifest to its after-digests (A2/B5 anchor spans swapped) + corrected 3 Stage-B stale literals in the normative body; P2/P3 stay false so readiness stays consistently BLOCKED (the P1 boundary crossed, not the whole gate). Inverted/re-homed 6 gate self-tests hard-pinned to the live Stage-A state — a new `P1Mutation.PreMigrationStatusAbsent` preserves the Stage-A schema-incomplete branch on a synthesized tree; updated the INV-014 trx-guard required-fixture list + committed `happy.trx` for the renamed anchor test. Gate green-from-clean 227/227, GATE_EXIT=0, banner PASS-BLOCKED; spike ADR consumers unaffected (Inv013 24/24). DRIFT-001/002/003 now execute (P1 no longer short-circuits) but remain fail-safe accepted debt.

### 2026-07-29 — P3 Determinism Attestation — PR1 (Group A)
Branch: feature/p3-determinism-attestation. Rules: 40 (30 INV + 10 PRH); PR1 built Group A (INV-001..005 + PRH-003/004/005). QA rounds: 5. Findings fixed: 10. Spike-side (non-production) PR1 of a 3-PR arc that turns the Phase-0.0 determinism check into a tamper-evident, CI-attested capability baseline: a total fail-closed status classifier over a committed closed `(execution × comparison)` legal table, a `DeterminismComparison` with committed-registry role/kind set-equality + a projection-policy pin (impl + schema digest), a dedicated ≥8-core serial CI lane (`p3-determinism-lane.yml` → `determinism-lane.sh`; pinned OS, from-clean, no rerun) that drives two nested `run-spike.sh` runs and emits a `RunReceipt` binding the observed platform identity, PRH-003 privacy scan wired fail-closed into `RunCli`, and a commit-anchored measurement-campaign scaffold. P3 stays false / readiness BLOCKED (PR1 signs nothing). Critical intensity: 5 QA rounds (a converging CI-exclusion / parallel-path saga QA-001→013, all mutation-proven) + full probe round (9 probes, 6 killed) + 3-round mini-audit (8 lenses; Tier-1 fixed: shipped-mapping fail-closed test, PRH-003 gate test, exit-table docs). Two post-`done` fixes: a pre-existing ETXTBSY launcher race (355137f) and the /cverify BLOCKING-1 out-of-tree `--run-root` fail-closed guard (12686b1). 17 Tier 2–4 mini-audit findings deferred (DF-013..029); QA-003 real measurement campaign is a HARD pre-landing requirement. Green-from-clean: spike 328/328 + carrier gate 228/228.
